// src/components/AddVisitor.js
import React from "react";
import { Typography } from "@mui/material";

const AddVisitor = () => (
    <Typography variant="h4">Add New Visitor</Typography>
);

export default AddVisitor;
